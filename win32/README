First time:
1) Make sure to install the mingw32 package
2) Install the dependencies in /opt/win/mingw using the 'install-dependencies.sh' script:

$ ./install-dependencies.sh /opt/win/mingw

Building:
1) Run ./mingw-configure
2) make && make install
