layout
{
  defines
  {
    distance ?= 2
    per_type ?= 1
  }

  [if(@per_type)]
  {
    type at $(@[@0+] * @distance), $(@0[?] * @distance)
  }

  [if($(@per_type ? 0 : 1))]
  {
    count at $(@0[?] * @distance), 0
  }
}
